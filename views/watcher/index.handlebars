<div class="container-fluid p-0 bg-light" style="min-height: calc(100vh - 56px);">
    <div class="p-2 bg-white border-bottom">
        {{#if user.admin }}
            <a href="/watcher/generate-invite">
                <button type="button" class="btn btn-outline-dark btn-sm">
                    <i class="fa fa-envelope mr-2"></i> Send Invite
                </button>
            </a>
        {{/if}}
    </div>


    <capture-canvas class="mt-3"></capture-canvas>
</div>